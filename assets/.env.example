# DM Auto OS — Environment variables (placeholders only)
# Copy to env.local.json for local development (gitignored).
# Never commit files containing real credentials.

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_STORAGE_DOCUMENTS_BUCKET=documents
SUPABASE_STORAGE_VEHICLE_PHOTOS_BUCKET=vehicle-photos
